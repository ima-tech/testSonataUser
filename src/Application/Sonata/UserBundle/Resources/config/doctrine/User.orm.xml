<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                  http://doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="Application\Sonata\UserBundle\Entity\User" table="fos_user_user">

        <id name="id" column="id" type="integer">
            <generator strategy="AUTO" />
        </id>

        <field name="address" column="address" type="string" length="200"/>
        <field name="city" column="city" type="string" length="100"/>
        <field name="state" column="state" type="string" length="100"/>
        <field name="country" column="country" type="string" length="100"/>
        <field name="postalCode" column="postalCode" type="string" length="20"/>

        <one-to-many field="children"
                     target-entity="User"
                     mapped-by="parent"
                />
        <many-to-one field="parent"
                     target-entity="User"
                     inversed-by="children"
                />
    </entity>

</doctrine-mapping>
